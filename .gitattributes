# Normalize line endings to LF in the working tree on every platform. Without
# this, the Windows CI runner checks out CRLF and the @antfu markdown formatter
# (Prettier) fails the lint step on files it sees as mixed-ending. text=auto
# still lets git treat detected binaries as binary.
* text=auto eol=lf
